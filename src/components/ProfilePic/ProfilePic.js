import React from 'react';
import styles from './ProfilePic.module.css';

const profilePic = () =>(
   <img src={'https://via.placeholder.com/600/92c952'} className={styles.ProfilePic} alt='profile_pic'/>
);

export default profilePic;